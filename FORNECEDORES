CREATE DATABASE Fornecedores;

CREATE TABLE FORN(
  ID int NOT NULL,
  NOME varchar(255),
  TEL int,
  EMAIL varchar(255),
  ATUAÇÃO varchar(255),
  STATUS int,
  PRIMARY KEY (ID)
);

CREATE TABLE CONTATO(
  ForID int NOT NULL,
  CONTATO varchar(255),
  FRIEND bool,
  NEGOCIAVEL bool,
  FOREIGN KEY (ForID) REFERENCES FORN(ID)
);

INSERT INTO FORN VALUES 
(1,'Lufix',NULL ,'atacado03@lufix.com.br','PARAFUSOS',1),
(2,'Dipal',NULL,'sintique@dipal.com.br','Parafusos',1),
(3,'Alvaro Parafusos',NULL,'alvaroparafusosltda@gmail.com','Parafusos',1),
(4, 'Palácio das Ferramentas',NULL,'edsonteixeira@palaciodasferramentas.com.br',1),
(5, 'ROMATA',NULL, 'leonardo@romata.com.br',1),
(6,'World Tools',NULL,'gabriel@worldtools.com.br',1);
